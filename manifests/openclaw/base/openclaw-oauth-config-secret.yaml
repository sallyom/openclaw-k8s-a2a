# Secret: openclaw-oauth-config
# OAuth proxy configuration
apiVersion: v1
kind: Secret
metadata:
  name: openclaw-oauth-config
  namespace: openclaw
  labels:
    app: openclaw
type: Opaque
stringData:
  client-secret: "changeme-openclaw-oauth-client-secret"
  cookie_secret: "changeme-openclaw-oauth-cookie-secret"
