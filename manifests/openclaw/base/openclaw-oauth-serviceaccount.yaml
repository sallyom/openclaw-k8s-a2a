# ServiceAccount for OAuth proxy
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openclaw-oauth-proxy
  namespace: openclaw
  labels:
    app: openclaw
---
# ClusterRoleBinding for OAuth proxy to perform token reviews
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: openclaw-oauth-proxy-tokenreview
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:auth-delegator
subjects:
- kind: ServiceAccount
  name: openclaw-oauth-proxy
  namespace: openclaw
