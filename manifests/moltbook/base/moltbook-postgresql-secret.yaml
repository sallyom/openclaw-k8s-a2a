# Secret: moltbook-postgresql
# Extracted from OpenShift template
apiVersion: v1
kind: Secret
metadata:
  name: moltbook-postgresql
  namespace: moltbook
  labels:
    app: moltbook
    component: database
type: Opaque
stringData:
  database-name: moltbook
  database-user: moltbook
  database-password: "changeme-generate-random-postgres-password"
